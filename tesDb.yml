config:
  target: "http://localhost:3000"
  phases:
    - duration: 60      # run for 60 second
      arrivalRate: 20   # 20 new user in each second
      name: Warm up phase
scenarios:
  - flow:
      - get:
          url: "/api/db-connect-check"